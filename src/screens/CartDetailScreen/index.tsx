import React from 'react';
import {Text} from 'react-native';
import {Screen} from 'common';

interface CartDetailScreenProps {}

export const CartDetailScreen: React.FC<CartDetailScreenProps> = () => {
  return (
    <Screen>
      <Text>CartDetailScreen</Text>
    </Screen>
  );
};
